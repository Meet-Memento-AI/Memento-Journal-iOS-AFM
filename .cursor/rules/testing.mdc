---
description: When to add or update regression tests for MeetMemento
globs:
  - MeetMemento/**/*ChatViewModel*.swift
  - MeetMemento/**/*ChatService*.swift
  - MeetMemento/**/*Onboarding*.swift
  - MeetMemento/Services/Intelligence/**/*.swift
---

# Regression tests

When you change **chat behavior**, the **onboarding flow**, or the **on-device intelligence layer**, add or update tests in the **same PR**.

## Commands

- **iOS (unit + UI):**
  `xcodebuild -scheme MeetMemento -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' test`
  If the destination is not found, pick an available iPhone simulator from `xcodebuild -showdestinations -scheme MeetMemento`.

## Conventions

- Prefer descriptive test names: `test_<Area>_<behavior>()`.
- Use `ChatServiceProtocol` + `MockChatService` for `ChatViewModel` unit tests; avoid live network in unit tests.
- For UI smoke tests, rely on stable `accessibilityIdentifier` values (e.g. `welcome.root`).
